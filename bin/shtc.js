#!/usr/bin/env node
/**
 * SHTC CLI — Simple Hyper Text Components
 * ==================================
 * CLI entry point.
 * Usage: shtc [command] [options]
 */
import { cli } from '../core/index.js';
cli(process.argv);
